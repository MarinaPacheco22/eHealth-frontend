
export class Medico {

  constructor(
    id ?: number,
    nombre ?: string,
    apellidos ?: string,
    dni ?: string,
    fechaNacimiento ?: string,
    activo ?: boolean,
    telefono ?: number,
    password ?: string,
    email ?: string,
    sexo ?: string,
    numeroDeColegiado ?: string,
    especialidad ?: string,
    pacientesAsignados ?: []
    ) {
  }
}
