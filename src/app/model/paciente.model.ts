
export class Paciente {

  constructor(
    id ?: number,
    nombre ?: string,
    apellidos ?: string,
    dni ?: string,
    fechaNacimiento ?: string,
    numSegSocial ?: string,
    altura ?: number,
    peso ?: number,
    telefono ?: number,
    contrasena ?: string,
    sexo ?: string,
    email ?: string,
    historialClinicoId ?: Number,
    medicoAsignadoId ?: Number
    ) {
  }
}
