
export class Medico {

  constructor(
    nombre ?: string,
    apellidos ?: string,
    dni ?: string,
    fechaNacimiento ?: string,
    activo ?: boolean,
    telefono ?: number,
    password ?: string,
    email ?: string,
    numeroDeColegiado ?: string,
    especialidad ?: string,
    pacientesAsignados ?: []
    ) {
  }
}
